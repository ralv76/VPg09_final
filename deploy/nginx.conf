# Фрагмент конфигурации NGINX для Генератора подкастов. ТЗ 3.7: статика через NGINX, файлы только через backend.
# Включить в server { } или использовать как reference.

# Прокси на приложение (Flask/gunicorn)
location / {
    proxy_pass http://127.0.0.1:5000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_read_timeout 600s;
    proxy_send_timeout 600s;
}

# WebSocket
location /ws {
    proxy_pass http://127.0.0.1:5000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
}

# Не отдаём файлы подкастов напрямую — только через backend (ТЗ 3.7)
# Каталог storage не должен быть в document_root.
